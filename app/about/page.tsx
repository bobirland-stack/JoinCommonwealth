/* About — placeholder route (Phase 1). Content arrives in a later phase. */
export default function AboutPage() {
  return (
    <main style={{ padding: "2rem", maxWidth: 720, margin: "0 auto" }}>
      <h1>About</h1>
      <p className="meta">About — placeholder route. Content arrives in a later phase.</p>
    </main>
  );
}
