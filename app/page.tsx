/* Home — placeholder route (Phase 1). Content arrives in Phase 2. */
export default function HomePage() {
  return (
    <main style={{ padding: "2rem", maxWidth: 720, margin: "0 auto" }}>
      <h1>Commonwealth</h1>
      <p className="meta">Home — placeholder route. Content arrives in Phase 2.</p>
    </main>
  );
}
