/* Transparency — placeholder route (Phase 1). Content arrives in a later phase. */
export default function TransparencyPage() {
  return (
    <main style={{ padding: "2rem", maxWidth: 720, margin: "0 auto" }}>
      <h1>Transparency</h1>
      <p className="meta">Transparency — placeholder route. Content arrives in a later phase.</p>
    </main>
  );
}
