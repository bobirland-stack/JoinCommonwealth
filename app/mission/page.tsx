/* Mission — placeholder route (Phase 1). Content arrives in a later phase. */
export default function MissionPage() {
  return (
    <main style={{ padding: "2rem", maxWidth: 720, margin: "0 auto" }}>
      <h1>Mission</h1>
      <p className="meta">Mission — placeholder route. Content arrives in a later phase.</p>
    </main>
  );
}
