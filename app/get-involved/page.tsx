/* Get involved — placeholder route (Phase 1). Content arrives in a later phase. */
export default function GetInvolvedPage() {
  return (
    <main style={{ padding: "2rem", maxWidth: 720, margin: "0 auto" }}>
      <h1>Get involved</h1>
      <p className="meta">Get involved — placeholder route. Content arrives in a later phase.</p>
    </main>
  );
}
