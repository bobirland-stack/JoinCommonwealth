/* Trust & honesty — placeholder route (Phase 1). Content arrives in a later phase. */
export default function TrustPage() {
  return (
    <main style={{ padding: "2rem", maxWidth: 720, margin: "0 auto" }}>
      <h1>Trust & honesty</h1>
      <p className="meta">Trust & honesty — placeholder route. Content arrives in a later phase.</p>
    </main>
  );
}
