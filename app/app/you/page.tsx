/* You — resident-app tab (Phase 1 placeholder). */
export default function YouPage() {
  return (
    <main style={{ padding: "1.5rem", maxWidth: 640, margin: "0 auto" }}>
      <h1>You</h1>
      <p className="meta">You — placeholder tab. Surfaces arrive in a later phase.</p>
    </main>
  );
}
