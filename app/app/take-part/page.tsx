/* Take part — resident-app tab (Phase 1 placeholder). */
export default function TakePartPage() {
  return (
    <main style={{ padding: "1.5rem", maxWidth: 640, margin: "0 auto" }}>
      <h1>Take part</h1>
      <p className="meta">Take part — placeholder tab. Surfaces arrive in a later phase.</p>
    </main>
  );
}
