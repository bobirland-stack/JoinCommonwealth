/* Government — resident-app tab (Phase 1 placeholder). */
export default function GovernmentPage() {
  return (
    <main style={{ padding: "1.5rem", maxWidth: 640, margin: "0 auto" }}>
      <h1>Government</h1>
      <p className="meta">Government — placeholder tab. Surfaces arrive in a later phase.</p>
    </main>
  );
}
