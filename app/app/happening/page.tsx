/* Happening — resident-app tab (Phase 1 placeholder). */
export default function HappeningPage() {
  return (
    <main style={{ padding: "1.5rem", maxWidth: 640, margin: "0 auto" }}>
      <h1>Happening</h1>
      <p className="meta">Happening — placeholder tab. Surfaces arrive in a later phase.</p>
    </main>
  );
}
