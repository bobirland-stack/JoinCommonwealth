/* Your Town — resident-app tab (Phase 1 placeholder). */
export default function YourTownPage() {
  return (
    <main style={{ padding: "1.5rem", maxWidth: 640, margin: "0 auto" }}>
      <h1>Your Town</h1>
      <p className="meta">Your Town — placeholder tab. Surfaces arrive in Phase 3.</p>
    </main>
  );
}
