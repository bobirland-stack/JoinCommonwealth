/* Settings — placeholder route (Phase 1). Content arrives in a later phase. */
export default function SettingsPage() {
  return (
    <main style={{ padding: "2rem", maxWidth: 720, margin: "0 auto" }}>
      <h1>Settings</h1>
      <p className="meta">Settings — placeholder route. Content arrives in a later phase.</p>
    </main>
  );
}
